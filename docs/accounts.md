# アカウント一覧

`accounts.html` には、利用している外部サービスのアカウントへのリンクを掲載します。

## 更新方法

アカウントの追加、変更、削除は `accounts.html` のアカウント一覧を直接編集します。ただし、`component:sidebar` マーカーで囲まれたサイドバーは自動生成されるため編集しないでください。

更新後はサイトをビルドします。

```sh
npm run build
```
