# トップ画像

トップ画像の元ファイルは `image-src/profile-doll.jpg` です。

`npm run build` を実行すると、最大幅1280px、品質82のWebPへ変換し、`images/profile-doll.webp` に出力します。トップページは生成されたWebPを参照します。

```sh
npm run build
```

生成された `images/profile-doll.webp` は直接編集しないでください。
